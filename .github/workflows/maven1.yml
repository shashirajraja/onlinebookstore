name: Java CI with Maven

on:
  push:
    branches: [ "master" ]

jobs:
  build:

    runs-on: self-hosted 

    steps:
    - uses: actions/checkout@v3
    - name: java-version
      run: java -version
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name:  deploy
      run: curl -v -u admin:admin -T /home/ec2-user/_work/onlinebookstore/onlinebookstore/target/dependency/webapp-runner.jar'http://34.201.71.186:8080/manager/text/deploy/?path=/onlinebook123&update=true'
